# Installation Guide

Backend OLT / ISP NMS

---

# System Requirements

## Operating System

Recommended:

```text
Ubuntu Server 20.04 LTS
```

Minimum:

```text
Ubuntu 20.04+
```

---

# Hardware Requirements

## Small ISP

```text
CPU      : 2 Core
RAM      : 4 GB
Storage  : 40 GB SSD
```

Recommended:

```text
CPU      : 4 Core
RAM      : 8 GB
Storage  : 100 GB SSD
```

---

# Required Software

## Node.js

Version:

```text
Node.js 22 LTS
```

Check:

```bash
node -v
```

Expected:

```text
v22.x.x
```

---

## NPM

Check:

```bash
npm -v
```
## LOGS PORT 541
JALANKAN NODE WAJIB SUPERUSER
---

## PostgreSQL

## versi lapangan aku
```sql
postgres=# CREATE DATABASE "backendOlt";
CREATE DATABASE
postgres=# CREATE USER jawara WITH PASSWORD 'BackendOlt123';
CREATE ROLE
postgres=# GRANT ALL PRIVILEGES ON DATABASE "backendOlt" TO jawara;
GRANT
postgres=# ALTER DATABASE "backendOlt" OWNER TO jawara;
ALTER DATABASE
postgres=# \q

DATABASE_URL="postgresql://jawara:BackendOlt123@localhost:5432/backendOlt"
```

Version:

```text
PostgreSQL 16+
```

Check:

```bash
psql --version
```

---

## PM2

Install:

```bash
npm install -g pm2
```

Check:

```bash
pm2 -v
```

---

## Nginx

Install:

```bash
sudo apt install nginx
```

Check:

```bash
nginx -v
```

---

# Install PostgreSQL

## Update System

```bash
sudo apt update
sudo apt upgrade -y
```

---

## Install PostgreSQL

```bash
sudo apt install postgresql postgresql-contrib -y
```

---

## Login PostgreSQL

```bash
sudo -u postgres psql
```

---

## Create Database

```sql
CREATE DATABASE backendolt;
```

---

## Create User

```sql
CREATE USER backendolt
WITH PASSWORD 'StrongPassword';
```

---

## Grant Access

```sql
GRANT ALL PRIVILEGES
ON DATABASE backendolt
TO backendolt;
```

---

# Clone Project

Directory:

```text
/opt/backend-olt
```

Create:

```bash
sudo mkdir -p /opt/backend-olt
```

Clone:

```bash
git clone <repository-url> /opt/backend-olt
```

---

# Backend Installation

## Enter Backend

```bash
cd /opt/backend-olt/backend
```

---

## Install Dependencies

```bash
npm install
```

---

## Create Environment

```bash
cp .env.example .env
```

Edit:

```env
PORT=3000

DATABASE_URL="postgresql://backendolt:StrongPassword@localhost:5432/backendolt"

JWT_SECRET="CHANGE_ME"

BASE_URL="https://nms.domain.com"
```

---

## Generate Prisma Client

```bash
npx prisma generate
```

---

## Run Migration

```bash
npx prisma migrate deploy
```

---

## Seed Database

If available:
karna aku telah konfigurasi 
```bash
{
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}

jadi cukup

npx prisma db seed
```

---

## Build Backend

```bash
npm run build
```

---

## Start Backend

```bash
npm start
```

Backend:

```text
http://localhost:3000
```

---

# Frontend Installation

## Enter Frontend

```bash
cd /opt/backend-olt/web
```

---

## Install Dependencies

```bash
npm install
```

---

## Create Environment
saat ini masih opsional

```bash
cp .env.example .env
```

Edit:

```env
VITE_API_URL=/api
```

---

## Build Frontend

```bash
npm run build
```

Output:

```text
dist/
```

---

# Deploy Frontend

Create directory:

```bash
sudo mkdir -p /var/www/backend-olt
```

Copy build:

```bash
sudo cp -R dist/* /var/www/backend-olt/
```

---

# Configure PM2

## Start Backend

```bash
cd /opt/backend-olt/backend
```

```bash
pm2 start dist/server.js \
  --name backend-olt
```

---

## Save Process

```bash
pm2 save
```

---

## Enable Startup

```bash
pm2 startup
```

Run command generated by PM2.

Then:

```bash
pm2 save
```

---

# Configure Nginx

Create:

```bash
sudo nano \
/etc/nginx/sites-available/backend-olt
```

Configuration:

```nginx
server {

    listen 80;

    server_name nms.domain.com;

    root /var/www/backend-olt;

    index index.html;

    location / {

        try_files $uri $uri/ /index.html;

    }

    location /api {

        proxy_pass http://127.0.0.1:3000;

        proxy_http_version 1.1;

        proxy_set_header Host $host;

        proxy_set_header X-Real-IP $remote_addr;

        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_set_header X-Forwarded-Proto $scheme;

    }

}
```

Enable:

```bash
sudo ln -s \
/etc/nginx/sites-available/backend-olt \
/etc/nginx/sites-enabled/
```

---

## Test Configuration

```bash
sudo nginx -t
```

---

## Reload Nginx

```bash
sudo systemctl reload nginx
```

---

# SSL Certificate

Install Certbot:

```bash
sudo apt install certbot \
python3-certbot-nginx -y
```

Generate SSL:

```bash
sudo certbot --nginx
```

Follow wizard.

---

# Verify Services

Backend:

```bash
pm2 status
```

PostgreSQL:

```bash
sudo systemctl status postgresql
```

Nginx:

```bash
sudo systemctl status nginx
```

---

# Update Application

## Backend

```bash
cd /opt/backend-olt/backend

git pull

npm install

npm run build

npx prisma migrate deploy

pm2 restart backend-olt
```

---

## Frontend

```bash
cd /opt/backend-olt/web
git pull
npm install
npm run build
sudo rsync -av dist/ /var/www/backend-olt/
sudo systemctl reload nginx
```

---

# Backup Database

Manual:

```bash
pg_dump \
-U backendolt \
backendolt \
> backup.sql
```

Restore:

```bash
psql \
-U backendolt \
backendolt \
< backup.sql
```

---

# Production Architecture

```text
Internet
    │
    ▼
Nginx :80 / :443
    │
    ├── Frontend React
    │
    └── /api
          │
          ▼
      Fastify :3000
          │
          ▼
      PostgreSQL
```

---

# Default Ports

```text
Frontend      : 80 / 443
Backend API   : 3000
PostgreSQL    : 5432
```

---

# Services

```text
nginx
postgresql
backend-olt (PM2)
```


## webhook url telegram
saatini tambahkan /api setelah domain
```json
misal ambil chontoh
domain : https://domainaku.com

yang jadi webhook di web dev adalah https://domainku.com/api
```